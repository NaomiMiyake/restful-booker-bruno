info:
  name: API-007 Get nonexistent booking
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseurl}/booking/999999999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
