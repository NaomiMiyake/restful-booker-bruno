info:
  name: API-005 Delete booking
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseurl}}/booking/{{bookingId}}"
  headers:
    - name: Accept
      value: application/json
    - name: Cookie
      value: token={{token}};
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 201", function () {
            expect(res.status).to.equal(201);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
