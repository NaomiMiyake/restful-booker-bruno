info:
  name: API-015 Delete nonexistent booking
  type: http
  seq: 15

http:
  method: DELETE
  url: "{{baseurl}}/booking/999999999"
  headers:
    - name: Accept
      value: application/json
    - name: Cookie
      value: token={{token}};
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 405", function () {
            expect(res.status).to.equal(405);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
